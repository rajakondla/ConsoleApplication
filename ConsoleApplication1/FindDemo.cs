using System;
using System.Collections.Generic;

namespace ConsoleApplication1
{
    class FindDemo
    {
        // List<int> list = new List<int>(new int[] { 19, 23, 29 });

        //// Finds first element greater than 20
        ////instead of using foreach and if statement if we use Find() method with lamda expression for list 
        //List<int> result = list.Find(item => item > 20);
        //Console.WriteLine(result);
    }
}
